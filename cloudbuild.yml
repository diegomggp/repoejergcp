steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - mifuncion
  - --region=europe-west1
  - --source=./mifuncion
  - --trigger-bucket=mibucket
  - --runtime=python 312
  - --allow-unauthenticated